{{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="form-group">
        <label class="control-label col-sm-2" for="name">Link:</label>
            <div class="col-sm-10">{{ form_widget(form.original) }}</div>
    </div>
    
    <button class="btn btn-default">{{ button_label|default('Submit') }}</button>
{{ form_end(form) }}
